engine-strict=true
# Wrangler's local Miniflare dependency includes sharp, which has no Node 26
# prebuilt binary in this toolchain. WSMC does not use image processing, and
# Wrangler's D1/local server work correctly without running native installers.
ignore-scripts=true
